import React from 'react'

function NewItemButton(props) {
  return <button type="button" onClick={props.onClick} className="btn btn-primary btn-circle btn-md"></button>
}

export default NewItemButton
